# Derived from atheory-ai/release-template v0.2.3
# See .release-template-config for substitution values.
#
# Uses CGO=1 with Zig-backed cross-compilation (tree-sitter dep).

version: 2

project_name: ce

before:
  hooks:
    - go mod tidy

builds:
  - id: ce
    main: ./cmd/ce
    binary: ce
    env:
      - CGO_ENABLED=1
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    overrides:
      - goos: linux
        goarch: amd64
        env:
          - CC=zig cc -target x86_64-linux-gnu
      - goos: linux
        goarch: arm64
        env:
          - CC=zig cc -target aarch64-linux-gnu
      - goos: darwin
        goarch: amd64
        env:
          - CC=clang -arch x86_64
      - goos: darwin
        goarch: arm64
        env:
          - CC=clang -arch arm64
      - goos: windows
        goarch: amd64
        env:
          - CC=zig cc -target x86_64-windows-gnu
      - goos: windows
        goarch: arm64
        env:
          - CC=zig cc -target aarch64-windows-gnu
    ldflags:
      - -s -w
        -X main.version={{.Version}}

archives:
  - id: ce
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - LICENSE

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - Merge pull request
      - Merge branch

release:
  github:
    owner: atheory-ai
    name: context-engine
  draft: false
  prerelease: auto

brews:
  - name: ce
    repository:
      owner: atheory-ai
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: "https://atheory.ai"
    description: "Context Engine — AI-powered codebase intelligence"
    license: "Apache-2.0"
    install: |
      bin.install "ce"
    test: |
      system "#{bin}/ce", "version"
